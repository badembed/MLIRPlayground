[project]
name = "my-mlir-playground"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mlir_wheel",
    "mlir-python-bindings"
]

[tool.uv]
find-links = ["https://llvm.github.io/eudsl"]
